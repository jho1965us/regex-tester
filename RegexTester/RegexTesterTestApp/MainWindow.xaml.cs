using System.Windows;

namespace RegexTesterTestApp
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// A standalone GUI mainly introduced to avoid long visual studio startup time during debugging
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
